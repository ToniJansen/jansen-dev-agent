# jansen_dev_agent — Python dependencies
# Install: pip3 install -r requirements.txt
# System deps (apt): ffmpeg
# After install: python3 -m playwright install chromium --with-deps

groq>=0.9.0
python-dotenv>=1.0.0
python-telegram-bot>=20.0
requests>=2.31.0
plotly>=6.0.0
kaleido==0.2.1
playwright>=1.40.0
pytest>=8.0.0
