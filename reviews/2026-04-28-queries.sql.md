# Agent Code Review — queries.sql

_Generated by `overnight_agent` at 02:00_

```
*🗄️ queries.sql — dialect: SQL — 19:35*
🔴 6C  🟡 4W  🔵 2I

⚠️ L9: Hardcoded sensitive data → `Flagged for review`
🔴 L3: DELETE without WHERE → `ADD WHERE clause to filter rows`
🔴 L9: Hardcoded sensitive data → `Remove hardcoded credentials`
🔴 L10: DROP without transaction or backup check → `Use transaction and check for backups`
🔴 L2: SQL Injection → `Use parameterized queries`
🟡 L1: SELECT * → `Select only necessary columns`
🟡 L4: No LIMIT on query → `Add LIMIT to restrict row count`
🟡 L6: Implicit type cast → `Use explicit type casting`
🟡 L7: N+1 pattern → `Use JOIN instead of loop`
🟡 L8: LIKE with leading wildcard → `Avoid leading wildcard in LIKE`
🔵 L11: Missing index → `Create index on joining columns`
🔵 L12: Aggregate without GROUP BY safety net → `Add GROUP BY clause for aggregate functions`
*NEEDS FIXES ❌*
_@jansen\_dev\_agent\_bot_
```
