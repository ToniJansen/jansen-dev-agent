# Agent Code Review — auth_service_bug.py

_Generated by `overnight_agent` at 02:00_

```
*🔍 auth_service.py — 20:15*
🔴 4C  🟡 2W  🔵 0I

🔴 L4: Credenciais hardcoded no DB_URL → `Use variáveis de ambiente: os.getenv("DB_URL")`
🔴 L7: SQL Injection via f-string → `Use parâmetros: db.execute("SELECT * FROM users WHERE username=%s", (username,))`
🔴 L9: MD5 não é seguro para senhas → `Use bcrypt ou hashlib.pbkdf2_hmac("sha256", ...)`
🔴 L18: SQL Injection em delete_user sem validação → `Use parâmetros: db.execute("DELETE FROM users WHERE id=%s", (user_id,))`
🟡 L10: Comparação com `is` em vez de `==` → `Substitua: if result["password"] == hashed:`
🟡 L13: Argumento mutável padrão `filters={}` → `Use: def get_users(role, filters=None)` e inicialize dentro da função

*NEEDS FIXES ❌*
_@jansen\_dev\_agent\_bot_
```
