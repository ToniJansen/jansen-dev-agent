# Agent Code Review — auth_service_bug.py

_Generated by `overnight_agent` at 02:00_

```
*🔍 auth_service.py — 18:13*
🔴 4C  🟡 2W  🔵 0I

🔴 L4: Credenciais hardcoded no DB_URL → `usar variável de ambiente: os.getenv("DB_URL")`
🔴 L7: SQL Injection via f-string → `usar query parametrizada: db.execute("SELECT * FROM users WHERE username=%s", (username,))`
🔴 L9: MD5 é inseguro para senhas → `usar bcrypt ou hashlib.scrypt/pbkdf2_hmac`
🔴 L19: SQL Injection sem validação em delete_user → `usar query parametrizada: db.execute("DELETE FROM users WHERE id=%s", (user_id,))`
🟡 L10: Comparação com `is` em vez de `==` → `if result["password"] == hashed:`
🟡 L13: Argumento mutável padrão `filters={}` → `def get_users(role, filters=None): filters = filters or {}`

*NEEDS FIXES ❌*
_@jansen\_dev\_agent\_bot_
```
