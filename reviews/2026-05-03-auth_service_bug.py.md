# Agent Code Review — auth_service_bug.py

_Generated by `overnight_agent` at 02:00_

```
*🔍 auth_service.py — 02:00*
🔴 4C  🟡 2W  🔵 0I

🔴 L4: Credenciais hardcoded no DB_URL → `Use variáveis de ambiente: os.getenv("DB_URL")`
🔴 L7: SQL Injection via f-string → `Use queries parametrizadas: db.execute("SELECT * FROM users WHERE username=%s", (username,))`
🔴 L9: MD5 é inseguro para senhas → `Use bcrypt ou argon2: bcrypt.checkpw(password.encode(), stored_hash)`
🔴 L18: SQL Injection em delete_user sem validação → `Use query parametrizada: db.execute("DELETE FROM users WHERE id=%s", (user_id,))`
🟡 L10: Operador `is` compara identidade, não valor → `Substitua por: if result["password"] == hashed:`
🟡 L13: Argumento mutável padrão `filters={}` compartilhado entre chamadas → `Substitua por: def get_users(role, filters=None)` e `if filters is None: filters = {}`

*NEEDS FIXES ❌*
_@jansen\_dev\_agent\_bot_
```
