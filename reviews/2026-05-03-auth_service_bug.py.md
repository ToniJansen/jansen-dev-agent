# Agent Code Review — auth_service_bug.py

_Generated by `overnight_agent` at 02:00_

```
*🔍 auth_service.py — 00:50*
🔴 4C  🟡 2W  🔵 0I

🔴 L4: Credenciais hardcoded no DB_URL → `Usar variável de ambiente: os.environ["DB_URL"]`
🔴 L7: SQL Injection via f-string → `Usar query parametrizada: db.execute("SELECT * FROM users WHERE username=%s", (username,))`
🔴 L9: MD5 é inseguro para senhas → `Substituir por: bcrypt.checkpw(password.encode(), stored_hash)`
🔴 L18: SQL Injection em delete_user sem validação → `Usar: db.execute("DELETE FROM users WHERE id=%s", (user_id,))`
🟡 L10: Comparação com `is` em vez de `==` → `Trocar: if result["password"] == hashed:`
🟡 L13: Argumento mutável padrão `filters={}` → `Trocar por: filters=None` e inicializar dentro da função com `if filters is None: filters = {}`

*NEEDS FIXES ❌*
_@jansen\_dev\_agent\_bot_
```
