# Agent Code Review — auth_service_bug.py

_Generated by `overnight_agent` at 02:00_

```
*🔍 auth_service.py — 21:16*
🔴 4C  🟡 2W  🔵 0I

🔴 L4: Credenciais hardcoded no código → `Use env vars: os.getenv("DB_URL")`
🔴 L7: SQL Injection via f-string → `Use parameterized query: db.execute("SELECT * FROM users WHERE username=%s", (username,))`
🔴 L9: MD5 é inseguro para senhas → `Use bcrypt: bcrypt.checkpw(password.encode(), stored_hash)`
🔴 L19: SQL Injection sem validação de input → `Use parameterized query: db.execute("DELETE FROM users WHERE id=%s", (user_id,))`
🟡 L10: `is` compara identidade, não valor → `Substitua por: if result["password"] == hashed:`
🟡 L13: Argumento mutável padrão `{}` compartilhado entre chamadas → `Substitua por: def get_users(role, filters=None)` e inicialize dentro da função

*NEEDS FIXES ❌*
_@jansen\_dev\_agent\_bot_
```
