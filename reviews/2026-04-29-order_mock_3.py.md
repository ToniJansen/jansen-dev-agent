# Agent Code Review — order_mock_3.py

_Generated by `overnight_agent` at 02:00_

```
*🔍 order_mock_3.py — 20:59*
🔴 2C  🟡 4W  🔵 2I

🔴 L4: URL interna hardcoded → `PAYMENT_URL = os.environ["PAYMENT_URL"]`
🔴 L5: Credencial/secret hardcoded → (linha 5 — remover e usar variável de ambiente)
🟡 L7: Requisição sem timeout → `requests.post(PAYMENT_URL, ..., timeout=10)`
🟡 L11: Log de resposta completa pode expor PII/dados sensíveis → `print(f"Charge status: {response.status_code}")`
🟡 L16: Requisição de reembolso sem timeout → `requests.post(..., timeout=10)`
🟡 L18: Log de erro bruto pode expor dados sensíveis → `print(f"Refund failed: status {r.status_code}")`
🔵 L24: Sem proteção contra KeyError em `r.json()["balance"]` → `return r.json().get("balance")`
🔵 L27: Resultado de `ping()` ignorado e sem timeout → `requests.get(..., timeout=5); # verificar retorno`

*NEEDS FIXES ❌*
_@jansen\_dev\_agent\_bot_
```
