# Agent Code Review — order_mock_2.py

_Generated by `overnight_agent` at 02:00_

```
*🔍 order_mock_2.py — 20:45*
🔴 2C  🟡 3W  🔵 1I

🔴 L7: Path Traversal em `read_invoice` → `filename = os.path.basename(filename); path = os.path.join(UPLOAD_DIR, filename); assert path.startswith(UPLOAD_DIR)`
🔴 L30: Path Traversal em `delete_temp` → `filename = os.path.basename(filename); path = os.path.join("/tmp", filename)`
🟡 L4: `UPLOAD_DIR` pode ser `None` → `UPLOAD_DIR = os.environ.get("UPLOAD_DIR", "/uploads"); assert UPLOAD_DIR`
🟡 L14: `load_session` sem validação do caminho → `session_file = os.path.realpath(session_file); assert session_file.startswith(ALLOWED_DIR)`
🟡 L19: `save_report` aceita `name` sem sanitização → `name = os.path.basename(name)` antes de montar o path
🔵 L19: Magic string `"/reports"` inline → definir como constante `REPORTS_DIR = "/reports"`

*NEEDS FIXES ❌*
_@jansen\_dev\_agent\_bot_
```
