# Agent Code Review — q_test_2.sql

_Generated by `overnight_agent` at 02:00_

```
*🗄️ q_test_2.sql — dialect: PostgreSQL — 20:18*
🔴 1C 🟡 4W 🔵 1I

🔴 L5: Cartesian Join — missing ON clause will cross-join all rows → `FROM orders o JOIN customers c ON o.customer_id = c.id`

🟡 L9: Missing LIMIT — unbounded monthly revenue report may return excessive rows → `ADD LIMIT 12` or apply a date range filter `WHERE created_at >= NOW() - INTERVAL '1 year'`

🟡 L14: Implicit Cast — comparing NUMERIC column with VARCHAR literal may bypass index → `WHERE total_amount = 1500`

🟡 L18: UNION Type Mismatch — `order_id` (INT) paired with `name` (TEXT), `created_at` (TIMESTAMP) misaligned with `name` (TEXT) → align types: `SELECT order_id::TEXT, created_at::TEXT FROM orders`

🟡 L23: ORDER BY Position — fragile reference, breaks silently on column reorder → `ORDER BY total_amount DESC`

🔵 L27: Redundant DISTINCT — `GROUP BY` already deduplicates; `DISTINCT` is a no-op here → `SELECT customer_id, COUNT(*) FROM orders GROUP BY customer_id`

*NEEDS FIXES ❌*
_@jansen\_dev\_agent\_bot_
```
