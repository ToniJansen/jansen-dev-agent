# Agent Code Review — q_test_2.sql

_Generated by `overnight_agent` at 02:00_

```
*🗄️ q_test_2.sql — dialect: PostgreSQL — 21:41*
🔴 1C 🟡 4W 🔵 1I

🔴 L5: Cartesian Join — missing ON clause crosses all rows → `FROM orders o JOIN customers c ON o.customer_id = c.id`

🟡 L9: No LIMIT on large aggregation — unbounded result set risk → `ADD LIMIT 1000` or paginate at application layer

🟡 L14: Implicit cast — `total_amount` is NUMERIC, comparing to VARCHAR literal → `WHERE total_amount = 1500`

🟡 L18: UNION with mismatched column types — `created_at` vs `name` causes implicit cast or runtime error → `SELECT id, CAST(name AS TEXT) ... UNION SELECT order_id, CAST(created_at AS TEXT)`

🟡 L23: ORDER BY positional reference — fragile, breaks silently on schema changes → `ORDER BY total_amount DESC`

🔵 L27: Redundant DISTINCT with GROUP BY — GROUP BY already deduplicates → `REMOVE DISTINCT`

*NEEDS FIXES ❌*
_@jansen\_dev\_agent\_bot_
```
