# Agent Code Review — auth_service.py

_Generated by `overnight_agent` at 02:00_

```
*🔍 auth_service.py — 02:54*
🔴 4C 🟡 2W 🔵 0I

🔴 L4: Credenciais hardcoded no código-fonte → Usar variável de ambiente: `DB_URL = os.getenv("DATABASE_URL")`
🔴 L7: SQL Injection via f-string → Usar parâmetros: `"SELECT * FROM users WHERE username=%s", (username,)`
🔴 L9: MD5 é inseguro para senhas → Substituir por: `bcrypt.hashpw(password.encode(), bcrypt.gensalt())`
🔴 L18: SQL Injection sem validação de entrada → Usar parâmetros: `db.execute("DELETE FROM users WHERE id=%s", (user_id,))`
🟡 L10: Comparação de identidade com `is` em vez de `==` → `if result["password"] == hashed:`
🟡 L13: Argumento mutável padrão `{}` compartilhado entre chamadas → `def get_users(role, filters=None): filters = filters or {}`

*NEEDS FIXES ❌*
_@jansen\_dev\_agent\_bot_
```
