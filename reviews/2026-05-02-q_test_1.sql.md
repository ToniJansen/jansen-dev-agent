# Agent Code Review — q_test_1.sql

_Generated by `overnight_agent` at 02:00_

```
*🗄️ q_test_1.sql — dialect: PostgreSQL — 14:49*
🔴 3C  🟡 3W  🔵 1I

🔴 L4: GRANT ALL sem restrição → `GRANT SELECT, INSERT, UPDATE ON specific_table IN SCHEMA public TO app_user;`
🔴 L7: UPDATE sem WHERE (afeta todas as linhas) → `UPDATE users SET is_active = 0 WHERE id = :target_id;`
🔴 L10: Credencial hardcoded detectada (L11) → `Usar variável de ambiente ou cofre de segredos; remover valor imediatamente`
🟡 L14: SELECT * expõe hash de senha e PII → `SELECT id, email, role, created_at FROM users WHERE role = 'admin';`
🟡 L17: SELECT * sem LIMIT em audit_log → `SELECT * FROM audit_log WHERE user_id > 0 LIMIT 100;`
🟡 L20: Comparação de senha em texto plano → `Usar função de hash segura (ex: crypt/bcrypt) no nível da aplicação; nunca comparar plaintext`
🔵 L23: Alias redundante (mesmo nome da coluna) → `SELECT email, created_at FROM users;`

*NEEDS FIXES ❌*
_@jansen\_dev\_agent\_bot_
```
