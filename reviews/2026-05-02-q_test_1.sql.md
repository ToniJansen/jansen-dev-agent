# Agent Code Review — q_test_1.sql

_Generated by `overnight_agent` at 02:00_

```
*🗄️ q_test_1.sql — dialect: PostgreSQL — 22:50*
🔴 3C 🟡 3W 🔵 1I

🔴 L4: GRANT ALL sem restrição → `GRANT SELECT, INSERT ON TABLE specific_table IN SCHEMA public TO app_user;`
🔴 L7: UPDATE sem WHERE (afeta todas as linhas) → `UPDATE users SET is_active = 0 WHERE id = :target_id;`
🔴 L10: Credencial em texto claro — L11 → `Remover valor; usar variável de ambiente ou secret manager`
🟡 L14: SELECT * expõe hash de senha e PII → `SELECT id, email, role FROM users WHERE role = 'admin';`
🟡 L17: SELECT * sem LIMIT em audit_log → `SELECT * FROM audit_log WHERE user_id > 0 LIMIT 500;`
🟡 L20: Comparação de senha em texto plano (bypass de hash) → `WHERE password = crypt('mypassword123', password)` *(ex: pgcrypto)*
🔵 L23: Aliases redundantes que repetem o nome original → `SELECT email, created_at FROM users;`

*NEEDS FIXES ❌*
_@jansen\_dev\_agent\_bot_
```
