# Agent Code Review — order_mock_review_1.py

_Generated by `overnight_agent` at 02:00_

```
*🔍 auth_module.py — Review*
🔴 3C 🟡 3W 🔵 2I

🔴 L13: Comparação de hash insegura → `use hmac.compare_digest() instead of ==`
🔴 L21: Expiração manual ignora verificação nativa do JWT → `remove manual exp check; jwt.decode() validates exp automatically with options={"verify_exp": True}`
🔴 L13: ADMIN_PASS armazenado como JWT decodificável — credencial exposta em camadas → `store only the password hash directly, not wrapped in a JWT`

🟡 L7-9: SECRET/ADMIN_PASS/ADMIN_SALT podem ser None silenciosamente → `add startup validation: if not SECRET: raise RuntimeError("JWT_SECRET not set")`
🟡 L42: pbkdf2 com 100000 iterações — aceitável mas abaixo do recomendado atual (600000 NIST 2023) → `increase iterations to 600000`
🟡 L33-38: `check_permissions` redefine Enum a cada chamada, ineficiente → `move UserRole Enum to module scope`

🔵 L31: Import dentro de função (`from enum import Enum`) → `move import to top of file`
🔵 L13: Linha excessivamente longa (>120 chars), difícil de revisar → `split into multiple lines for readability`

*NEEDS FIXES ❌*
_@jansen\_dev\_agent\_bot_
```
