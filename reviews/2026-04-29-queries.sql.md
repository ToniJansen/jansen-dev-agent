# Agent Code Review — queries.sql

_Generated by `overnight_agent` at 02:00_

```
*🗄️ queries.sql — dialect: SQL — 02:00*
🔴 5C  🟡 5W  🔵 2I

⚠️ L9: Sensitive data hardcoded → ` flag line number only`
🔴 L3: DELETE without WHERE → `DELETE FROM order_temp WHERE id = ?`
🔴 L9: Hardcoded sensitive data → ` flag line number only`
🔴 L10: DROP without transaction → `BEGIN TRANSACTION; DROP TABLE IF EXISTS orders_archive_2022; COMMIT;`
🔴 L2: SQL Injection → `use parameterized queries`
🔴 L5: Hardcoded customer ID → `UPDATE orders SET status = 'cancelled' WHERE customer_id = ?`
🔴 L1: Missing index → `CREATE INDEX idx_orders_status ON orders (status)`
🟡 L1: SELECT * → `SELECT order_id, status FROM orders WHERE status = 'pending'`
🟡 L4: No LIMIT → `SELECT order_id, customer_id, total_amount, created_at FROM orders WHERE created_at > '2020-01-01' LIMIT 100`
🟡 L6: Implicit type cast → `SELECT * FROM payments WHERE amount = 150.00`
🟡 L7: N+1 pattern → `SELECT o.order_id, c.name, c.email FROM orders o JOIN customers c ON o.customer_id = c.id WHERE o.status = 'active'`
🟡 L8: LIKE with leading wildcard → `SELECT order_id, product_name FROM order_items WHERE product_description LIKE '%keyboard%'`
🔵 L11: Missing index → `CREATE INDEX idx_orders_customer_email ON orders (customer_email)`
🔵 L12: Aggregate without GROUP BY → `SELECT customer_id, SUM(total_amount), MAX(created_at) FROM orders GROUP BY customer_id`

*NEEDS FIXES ❌*
_@jansen\_dev\_agent\_bot_
```
