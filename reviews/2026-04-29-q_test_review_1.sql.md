# Agent Code Review — q_test_review_1.sql

_Generated by `overnight_agent` at 02:00_

```
*🗄️ q_test_review_1.sql — dialect: ANSI SQL / PostgreSQL — 20:58*
🔴 0C  🟡 4W  🔵 3I

🟡 L3: Coluna `password` — nome sensível exposto no schema → `USE hashed_password or password_hash consistently`
🟡 L1: GRANT amplo sem restrição de colunas → `GRANT SELECT (id,email,role), INSERT, UPDATE ON users TO app_user`
🟡 L5: `SELECT` em `audit_log` sem filtro de data/user — pode retornar dados sensíveis arbitrários → `ADD WHERE clause (e.g., WHERE created_at >= NOW() - INTERVAL '30 days')`
🟡 L7: `SELECT email, created_at` sem WHERE — exposição em massa de e-mails → `ADD WHERE clause or tighten LIMIT`
🔵 L1: `TABLE` keyword no GRANT é redundante em alguns dialetos → `GRANT SELECT, INSERT, UPDATE ON users TO app_user`
🔵 L6: Coluna `hashed_password` na cláusula WHERE — considere índice dedicado → `CREATE INDEX idx_users_hashed_password ON users(hashed_password)`
🔵 L3-4: INSERT sem valor default para `created_at` → `ADD created_at = NOW() or DEFAULT in DDL`

*NEEDS FIXES ❌*
_@jansen\_dev\_agent\_bot_
```
