# Agent Code Review — q_test_review_1.sql

_Generated by `overnight_agent` at 02:00_

```
*🗄️ q_test_review_1.sql — dialect: ANSI SQL / PostgreSQL — 16:56*
🔴 0C  🟡 3W  🔵 3I

🟡 L3: Column `password` — storing plaintext or unverified hash → `-- ensure bcrypt/argon2 hash before INSERT; rename to hashed_password for clarity`

🟡 L6: Redundant query — `hashed_password` lookup may enable timing attacks or user enumeration → `-- use constant-time comparison; avoid exposing column in SELECT`

🟡 L5: `audit_log` filter `user_id > 0` is overly broad — may expose all records unintentionally → `WHERE user_id = ? -- parameterize for specific user lookup`

🔵 L1: `GRANT` is broad — `INSERT/UPDATE` on `users` by `app_user` should be reviewed per least-privilege principle → `-- consider restricting to specific columns only`

🔵 L7: `SELECT email, created_at` without `WHERE` clause (only `LIMIT`) — may expose user data unintentionally → `WHERE <condition> LIMIT 100`

🔵 L3–4: Column order in `INSERT` — no `id` or `created_at` default noted; ensure DB defaults are set → `-- verify DEFAULT values on schema`

*NEEDS FIXES ❌*
_@jansen\_dev\_agent\_bot_
```
