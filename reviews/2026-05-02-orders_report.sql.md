# Agent Code Review — orders_report.sql

_Generated by `overnight_agent` at 02:00_

```
*🗄️ orders_report.sql — dialect: PostgreSQL — 17:07*
🔴 3C  🟡 2W  🔵 1I

🔴 L8: SQL Injection via concatenação dinâmica → `Use EXECUTE ... USING $1, $2 com parâmetros vinculados`
🔴 L16: Transferência sem transação — estado inconsistente → `Envolver os dois UPDATEs em BEGIN ... COMMIT com tratamento de exceção`
🔴 L5: Precedência lógica errada (AND antes de OR) → `WHERE (status = 'active' OR status = 'pending') AND created_at > NOW() - INTERVAL '7 days'`
🟡 L12: HAVING sem GROUP BY — comportamento indefinido/inválido → `SELECT customer_id FROM orders GROUP BY customer_id HAVING COUNT(*) > 5`
🟡 L19: SELECT * expõe colunas sensíveis (card_last4, internal_notes) → `Listar colunas explícitas: SELECT id, customer_id, status, total FROM orders WHERE status = 'refunded'`
🔵 L1: Ausência de LIMIT nas queries de relatório — risco de varredura total → `Adicionar LIMIT ou paginação explícita`

*NEEDS FIXES ❌*
_@jansen\_dev\_agent\_bot_
```
